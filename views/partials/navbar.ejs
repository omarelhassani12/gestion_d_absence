<nav class="main-header navbar navbar-expand navbar-white navbar-light">
  <!-- Left navbar links -->
  <ul class="navbar-nav">
      <li class="nav-item">
          <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
      </li>
  </ul>

  <!-- Center navbar content for role 1 -->
  <% if (user.role == 1) { %>
  <ul class="navbar-nav mx-auto">
      <li class="nav-item d-flex justify-content-center align-items-center">
          Votre Groupe : <b class="ml-2 text-danger"><%= //user.groupName %></b>
      </li>
  </ul>
  <% } %>

  <!-- Right navbar links -->
  <ul class="navbar-nav ml-auto">
      <% if (user.role != 1) { %>
      <!-- Notifications Dropdown Menu -->
      <li class="nav-item dropdown">
          <a class="nav-link" data-toggle="dropdown" href="#">
              <i class="far fa-bell"></i>
              <span class="badge badge-danger navbar-badge"><%= Allnotfi = user.nonActiveCompetesCount + user.countStagiairesWithNonZeroAbsence %></span>
          </a>
          <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
              <span class="dropdown-item dropdown-header"><%= Allnotfi %> Notifications</span>
              <div class="dropdown-divider"></div>
              <!-- Comptes non actifs -->
              <a href="/user" class="dropdown-item">
                  <i class="fas fa-user-clock mr-2"></i>Comptes non actifs
                  <span class="float-right text-muted text-sm"><%= user.nonActiveCompetesCount %></span>
              </a>
              <!-- Stagiaires avec fonctions -->
              <a href="/absence/absences/function" class="dropdown-item">
                  <i class="fas fa-users mr-2"></i>Stagiaires avec fonctions
                  <span class="float-right text-muted text-sm"><%= user.countStagiairesWithNonZeroAbsence %></span>
              </a>
          </div>
      </li>
      <% } %>
      <li class="nav-item ml-5">
          <a class="btn btn-danger" href="/auth/logout">
              <i class="fas fa-sign-out-alt"></i> Logout
          </a>
      </li>
  </ul>
</nav>
