<!DOCTYPE html>
<html lang="en">
<%- include('partials/header') %>

<body class="hold-transition sidebar-mini layout-fixed">
    <div class="wrapper">
        <%- include('partials/navbar') %>
        <%- include('partials/aside') %>

        <div class="content-wrapper">
            <section class="content-header">
                <div class="container-fluid">
                    <div class="row mb-2">
                        <div class="col-sm-6">
                            <h1>Stagiaires</h1>
                        </div>
                        <div class="col-sm-6">
                            <ol class="breadcrumb float-sm-right">
                                <li class="breadcrumb-item"><a href="#">Home</a></li>
                                <li class="breadcrumb-item active">Stagiaires</li>
                            </ol>
                        </div>
                    </div>
                </div>
            </section>

            <section class="content">
                <div class="container-fluid">
                    <div class="row">
                        <div class="col-12">
                            <div class="card">
                                <div class="card-header">
                                    <div class="row">
                                        <div class="col-sm-12 text-right">
                                            <div class="col-auto ms-auto d-print-none">
                                                <div class="d-flex justify-content-end">
                                                    <a href="#" class="btn btn-primary d-none d-sm-inline-block"
                                                        data-bs-toggle="modal" data-bs-target="#modal-report"
                                                        id="new-stagiaire-button">
                                                        New Stagiaire
                                                    </a>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <!-- /.card-header -->
                                <div class="card-body">
                                    <table id="example1" class="table table-bordered table-striped">
                                        <thead>
                                            <tr>
                                                <th>CFE</th>
                                                <th>Nom</th>
                                                <th>Prénom</th>
                                                <th>N de Groupe</th>
                                                <th>Action</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% if (stagiaires.length === 0) { %>
                                              <tr>
                                                <td colspan="5" style="text-align:center">No stagiaire records found.</td>
                                              </tr>
                                            <% } else { %>
                                              <% stagiaires.forEach((stagiaire) => { %>
                                                <tr>
                                                  <td><%= stagiaire.CFE %></td>
                                                  <td><%= stagiaire.lastName %></td>
                                                  <td><%= stagiaire.firstName %></td>
                                                  <td><%= stagiaire.groupId !== null ? 'group ' + stagiaire.groupId : 'No group' %></td>
                                                  <td>
                                                    <a href="#" class="btn btn-success" onclick="populateUpdateModal('<%= stagiaire.CFE %>', '<%= stagiaire.firstName %>', '<%= stagiaire.lastName %>', '<%= stagiaire.groupId %>', '<%= stagiaire.id %>')" data-bs-toggle="modal" data-bs-target="#modal-update">
                                                      <i class="fas fa-edit"></i>
                                                    </a>
                                                    <form id="delete-form-<%= stagiaire.id %>" action="/stagiaires/<%= stagiaire.id %>" method="POST" style="display: inline;">
                                                        <input type="hidden" name="_method" value="DELETE">
                                                        <button class="btn btn-danger delete-btn" onclick="confirmDelete(event, '<%= stagiaire.id %>')">
                                                          <i class="fas fa-trash-alt"></i>
                                                        </button>
                                                      </form>                                                    
                                                  </td>
                                                </tr>
                                              <% }) %>
                                            <% } %>
                                          </tbody>
                                    </table>
                                </div>
                                <!-- /.card-body -->
                            </div>
                            <!-- /.card -->
                        </div>
                        <!-- /.col -->
                    </div>
                    <!-- /.row -->
                </div>
                <!-- /.container-fluid -->
            </section>
            <!-- /.content -->
        </div>
        <!-- /.content-wrapper -->

        <%- include('partials/script') %>
    </div>
    <!-- ./wrapper -->

    <!-- Modal -->
    <div class="modal fade" id="modal-report" tabindex="-1" role="dialog" aria-labelledby="modal-title" aria-hidden="true">
        <div class="modal-dialog modal-lg" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="modal-title">New Stagiaire</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <form id="create-stagiaire-form" action="/stagiaires" method="POST">
                    <div class="modal-body">
                        <div class="mb-3">
                            <label class="form-label">CFE</label>
                            <input type="text" class="form-control" name="CFE" placeholder="CFE">
                        </div>
                        <div class="mb-3">
                            <label class="form-label">First Name</label>
                            <input type="text" class="form-control" name="firstName" placeholder="First Name">
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Last Name</label>
                            <input type="text" class="form-control" name="lastName" placeholder="Last Name">
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Group ID</label>
                            <input type="text" class="form-control" name="groupId" placeholder="Group ID">
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                        <button type="submit" class="btn btn-primary" id="create-stagiaire-button">Create Stagiaire</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!-- JavaScript code -->
    <!-- <script>
        document.addEventListener('DOMContentLoaded', function () {
            // When the "New Stagiaire" button is clicked, show the modal
            document.getElementById('new-stagiaire-button').addEventListener('click', function () {
                const modal = new bootstrap.Modal(document.getElementById('modal-report'));
                modal.show();
            });

            // Handle form submission
            document.getElementById('create-stagiaire-form').addEventListener('submit', function (event) {
                event.preventDefault(); // Prevent the form from submitting

                // Get the form data
                const form = document.getElementById('create-stagiaire-form');
                const formData = new FormData(form);

                // Send the form data via AJAX
                fetch('/stagaire/create', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json' // Add the Content-Type header
                    },
                    body: JSON.stringify(Object.fromEntries(formData)) // Convert FormData to JSON
                })
                    .then(response => response.json())
                    .then(data => {
                        // Rest of the code remains the same
                        if (data.success) {
                            // Stagiaire created successfully
                            alert('Stagiaire created successfully');
                            window.location.href = window.location.href; // Reload the page
                        } else {
                            // Error creating stagiaire
                            console.error('An error occurred while creating the stagiaire: ' + data.error);
                        }
                    })
                    .catch(error => {
                        console.error(error);
                        console.error('An error occurred while creating the stagiaire');
                    })
                    .finally(() => {
                        const modalBackdrop = document.getElementsByClassName('modal-backdrop');
                        const modalContainer = document.getElementsByClassName('modal');

                        if (modalBackdrop.length > 0) {
                            modalBackdrop[0].parentNode.removeChild(modalBackdrop[0]); // Remove the modal backdrop from the DOM
                        }

                        if (modalContainer.length > 0) {
                            modalContainer[0].parentNode.removeChild(modalContainer[0]); // Remove the modal container from the DOM
                        }
                    });
            });

            // When the modal is hidden, reset the form
            document.getElementById('modal-report').addEventListener('hidden.bs.modal', function () {
                document.getElementById('create-stagiaire-form').reset();
            });
        });

        // Handle delete button click
        function confirmDelete(event, stagiaireId) {
            event.preventDefault(); // Prevent form submission
            if (confirm("Are you sure you want to delete this record?")) {
                fetch(`/stagaire/${stagiaireId}`, {
                    method: 'DELETE'
                })
                    .then(response => response.json())
                    .then(data => {
                        if (data.success) {
                            // Stagiaire deleted successfully
                            alert('Stagiaire deleted successfully');
                            window.location.href = window.location.href; // Reload the page
                        } else {
                            // Error deleting stagiaire
                            console.error('An error occurred while deleting the stagiaire: ' + data.error);
                        }
                    })
                    .catch(error => {
                        console.error(error);
                        console.error('An error occurred while deleting the stagiaire');
                    });
            }
        }
    </script> -->

<!-- JavaScript code -->
<script>
    document.addEventListener('DOMContentLoaded', function () {
        // When the "New Stagiaire" button is clicked, show the modal
        document.getElementById('new-stagiaire-button').addEventListener('click', function () {
            const modal = new bootstrap.Modal(document.getElementById('modal-report'));
            modal.show();
        });

        // Handle form submission
        document.getElementById('create-stagiaire-form').addEventListener('submit', function (event) {
            event.preventDefault(); // Prevent the form from submitting

            // Get the form data
            const form = document.getElementById('create-stagiaire-form');
            const formData = new FormData(form);

            // Send the form data via AJAX
            fetch('/stagaire/create', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json' // Add the Content-Type header
                },
                body: JSON.stringify(Object.fromEntries(formData)) // Convert FormData to JSON
            })
                .then(response => response.json())
                .then(data => {
                    // Rest of the code remains the same
                    if (data.success) {
                        // Stagiaire created successfully
                        alert('Stagiaire created successfully');
                        window.location.href = window.location.href; // Reload the page
                    } else {
                        // Error creating stagiaire
                        console.error('An error occurred while creating the stagiaire: ' + data.error);
                    }
                })
                .catch(error => {
                    console.error(error);
                    console.error('An error occurred while creating the stagiaire');
                })
                .finally(() => {
                    const modalBackdrop = document.getElementsByClassName('modal-backdrop');
                    const modalContainer = document.getElementsByClassName('modal');

                    if (modalBackdrop.length > 0) {
                        modalBackdrop[0].parentNode.removeChild(modalBackdrop[0]); // Remove the modal backdrop from the DOM
                    }

                    if (modalContainer.length > 0) {
                        modalContainer[0].parentNode.removeChild(modalContainer[0]); // Remove the modal container from the DOM
                    }
                });
        });

        // When the modal is hidden, reset the form
        document.getElementById('modal-report').addEventListener('hidden.bs.modal', function () {
            document.getElementById('create-stagiaire-form').reset();
        });
    });

    // Handle delete button click
    function confirmDelete(event, stagiaireId) {
        event.preventDefault(); // Prevent form submission
        if (confirm("Are you sure you want to delete this record?")) {
            fetch(`/stagaire/${stagiaireId}`, {
                method: 'DELETE'
            })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        // Stagiaire deleted successfully
                        alert('Stagiaire deleted successfully');
                        window.location.href = window.location.href; // Reload the page
                    } else {
                        // Error deleting stagiaire
                        console.error('An error occurred while deleting the stagiaire: ' + data.error);
                    }
                })
                .catch(error => {
                    console.error(error);
                    console.error('An error occurred while deleting the stagiaire');
                });
        }
    }
</script>


</body>

</html>
